<Window x:Class="ServoAnimator.LibraryRangePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Library Item"
        Width="540" Height="330" MinWidth="420" MinHeight="280"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        PreviewKeyDown="Window_PreviewKeyDown"
        Background="{DynamicResource AppBackground}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Foreground="#C8CCD4" TextWrapping="Wrap"
                   Text="Drag the GREEN start and RED end arrows on the timeline. Add or edit the library item description, then click OK."/>
        <TextBlock Grid.Row="1" Text="Description:" Foreground="{DynamicResource HeaderText}"
                   FontWeight="Bold" Margin="0,10,0,4"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="2" MinLines="10"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Background="{DynamicResource PanelBackground}" Foreground="{DynamicResource PrimaryText}"
                 BorderBrush="{DynamicResource DividerBrush}" Padding="8"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="86" Padding="0,4" Margin="0,0,8,0"
                    IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="86" Padding="0,4"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
