# UI Dock Recovery v1.7.3

- About now shows `Designed by Mark Kovalcson` immediately below the version line.
- While the URDF is undocked, a `Dock` button appears below Headtop Controls in the right servo-grid column.
- The recovery button calls the same `SetUrdfUndocked(false)` path used by the detached URDF window.
